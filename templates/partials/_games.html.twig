
<div class="card-game">
    <div class="container-image {% if oui is defined %}low-height{% endif %}">
        <img src="{{ game.thumbnailCover }}" alt="{{ game.name }}">
        <div class="container-price">
            {{ game.price }}€
        </div>
    </div>
    <div class="card-infos">
        <p>{{ game.name }}</p>
    </div>
</div>